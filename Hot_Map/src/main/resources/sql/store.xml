<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper할때 써야됨 -->
<mapper namespace="store">
	<insert id="storeInsert">
		INSERT INTO store (name, menu, price, address, phone, opening_time, closing_time, date, mem_id)
		VALUES (#{name},#{menu},#{price},#{address},#{phone},#{opening_time},#{closing_time},now(),#{mem_id})
	</insert>


</mapper>
